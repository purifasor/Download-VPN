{
  "name": "professor-vpn-download",
  "version": "1.0.0",
  "private": true,
  "description": "Dark neon auto-updating download page (serverless, source-hidden)",
  "scripts": {
    "dev": "wrangler pages dev public --port 3000 --ip 0.0.0.0",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
